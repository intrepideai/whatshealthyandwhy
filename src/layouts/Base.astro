---
import UserNav from '../components/UserNav.astro';
import ThemeToggle from '../components/ThemeToggle.astro';
import EmailSubscribe from '../components/EmailSubscribe.astro';

interface Props {
  title: string;
  description?: string;
}

const { title, description = "Peer-reviewed health research, made accessible." } = Astro.props;
const siteTitle = "What's Healthy and Why";
const fullTitle = title === siteTitle ? title : `${title} — ${siteTitle}`;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <title>{fullTitle}</title>

    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <link rel="manifest" href="/manifest.json" />
    <meta name="theme-color" content="#2a7d4f" />
    <link rel="preload" href="/fonts/inter-regular.woff2" as="font" type="font/woff2" crossorigin />

    <meta property="og:title" content={fullTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content={siteTitle} />

    <!-- Circadian theme + dark mode override: runs before paint to avoid flash -->
    <script is:inline>
      (function() {
        var h = new Date().getHours();
        var t;
        if (h >= 21 || h < 5) t = 'night';
        else if (h >= 5 && h < 7) t = 'dawn';
        else if (h >= 7 && h < 17) t = 'day';
        else if (h >= 17 && h < 19) t = 'dusk';
        else t = 'evening';
        document.documentElement.setAttribute('data-time', t);
        var override = localStorage.getItem('theme-override');
        if (override) document.documentElement.setAttribute('data-theme', override);
      })();
    </script>
  </head>
  <body>
    <header class="site-header">
      <div class="header-inner">
        <a href="/" class="site-logo">What's Healthy & Why</a>
        <nav class="site-nav">
          <a href="/">Topics</a>
          <a href="/schedule">Schedule</a>
          <a href="/levels">Levels</a>
          <a href="/products">Products</a>
          <a href="/about">About</a>
        </nav>
        <UserNav />
        <ThemeToggle />
      </div>
    </header>

    <main class="site-main">
      <slot />
    </main>

    <EmailSubscribe />

    <footer class="site-footer">
      <p>&copy; {new Date().getFullYear()} What's Healthy and Why. All content backed by peer-reviewed research.</p>
      <p class="footer-links">
        <a href="/about">About</a>
        <span class="footer-sep">&middot;</span>
        <a href="/levels">Levels</a>
        <span class="footer-sep">&middot;</span>
        <a href="/contribute">Contribute</a>
        <span class="footer-sep">&middot;</span>
        <a href="https://github.com/intrepideai/whatshealthyandwhy" target="_blank" rel="noopener noreferrer">GitHub</a>
      </p>
    </footer>
    <script is:inline>
      if ('serviceWorker' in navigator) {
        navigator.serviceWorker.register('/sw.js').catch(function() {});
      }
    </script>
  </body>
</html>

<style is:global>
  @import '../styles/global.css';
</style>
